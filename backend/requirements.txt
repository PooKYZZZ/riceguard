# =========================================================
# RiceGuard Backend Requirements
# =========================================================

# --- Core Web Framework ---
fastapi==0.103.2
uvicorn[standard]==0.23.2
starlette==0.27.0

# --- Data Validation ---
pydantic[email]==1.10.19
email-validator==2.1.0.post1
python-multipart==0.0.9

# --- Environment Variables ---
python-dotenv==1.0.1

# --- Database (MongoDB) ---
pymongo==4.6.3

# --- Security / Authentication ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# --- Machine Learning / Image Processing ---
# TensorFlow + NumPy matrix is tricky. On Python 3.13, skip TF (use ML stub).
tensorflow==2.20.0 ; python_version < "3.13"

# NumPy: use 1.26.x when TF might be installed, else use 2.x for 3.13+
numpy==1.26.4 ; python_version < "3.13"
numpy>=2.1,<3.0 ; python_version >= "3.13"

pillow==10.4.0
# --- Utilities / Optional Tools ---
requests==2.31.0
typing-extensions>=4.7.1